встроенный модуль util
util.inspect - вывод обекта
util.format - 
util.inherit - готовый кусок для наследования
===
console.log
console.error

===
встроенныйй модуль events
EventEmmiter - основной объект для работы с событиями в node.js

- обработчики отработают в точно назначенном порядке
- получить список обработчиков на  событие
- утечка памяти => максимум 10 обработчиков, потом выкинет предупреждение

===
создаем свой обработчик errorDimon и наследуем от new Error + пару прототипов + try/catch+++ готово
=================================================================
#14 - nodejs as web server

let ser = new http.Server - слушает ip port(ser.listen) и отвечает на входящие запросы